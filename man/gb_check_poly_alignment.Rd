% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stitch_fovs_to_ometiff.R
\name{gb_check_poly_alignment}
\alias{gb_check_poly_alignment}
\title{Report the polygon-to-raster transform for one slide}
\usage{
gb_check_poly_alignment(
  fov_positions,
  polygons,
  state_col = "fov",
  fov_size_px = 4256L,
  pixel_size_um = 0.120280945,
  fov_col = "fov",
  cell_col = "cell_ID",
  x_col = "x_slide_mm",
  y_col = "y_slide_mm",
  units = "auto",
  trim = 0.002
)
}
\arguments{
\item{fov_positions}{Path to that slide's \code{*_fov_positions_file.csv(.gz)}
(or a data.frame).}

\item{polygons}{A \pkg{Seurat} object, a polygon vertex data.frame, or an
\code{.rds} path to either. Must cover only the one slide.}

\item{state_col}{Any existing \code{meta.data} / table column; needed only to
satisfy the extractor, it plays no part in the fit.}

\item{fov_size_px, pixel_size_um}{As in \code{\link{stitch_fovs_to_ometiff}}.}

\item{fov_col, cell_col, x_col, y_col}{Column names in the polygon input.}

\item{units}{\code{"auto"} (default) to infer the coordinate units, or one of
\code{"mm"}, \code{"um"}, \code{"px"} to fix them.}

\item{trim}{Trimming quantile for the feasibility bounds.}
}
\value{
(Invisibly) the fit: a list with \code{units}, \code{ax}, \code{bx},
\code{ay}, \code{by}, the interval widths and the fraction of vertices
landing inside their own FOV.
}
\description{
Diagnostic companion to \code{\link{stitch_fovs_to_ometiff}}: fits and prints
the affine that maps a polygon input's coordinates onto the stitched raster
grid, without running a stitch. Useful for checking a new polygon source, or
for reading off the equivalent manual arguments.
}
\examples{
\dontrun{
gb_check_poly_alignment(
  "~/CosMX_data/run/flatFiles/MNCharu/MNCharu_fov_positions_file.csv.gz",
  "~/CosMX_data/run/seuratObject_MN.Charu.RDS", state_col = "fov")
}
}
