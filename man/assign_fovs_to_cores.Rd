% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_fovs_to_cores.R
\name{assign_fovs_to_cores}
\alias{assign_fovs_to_cores}
\title{Assign FOVs to TMA Cores with Strict Geographic Lock and Conditional Cohesion}
\usage{
assign_fovs_to_cores(
  fov_input,
  cell_input,
  tma_map_input,
  fov_size = 4256,
  core_drift_tolerance = 0.4,
  min_cells = 50,
  slidelabels = NULL
)
}
\arguments{
\item{fov_input}{Data source for FOVs. Must contain \code{x_global_px}, \code{y_global_px}, and \code{FOV}.}

\item{cell_input}{Data source for Cells. Must contain \code{CenterX_global_px}, \code{CenterY_global_px}, and \code{FOV}.}

\item{tma_map_input}{A matrix/dataframe representing the physical TMA layout.}

\item{fov_size}{Numeric. The size of the FOV in pixels. Default is \code{4256}.}

\item{core_drift_tolerance}{Numeric (0 to 1). Max fraction of grid spacing an anchor can drift. Default \code{0.40}.}

\item{min_cells}{Numeric. Minimum number of cells required to validate a core anchor. Default \code{50}.}

\item{slidelabels}{Optional character vector of slide names.}
}
\value{
A named list containing \code{mapped_data} and \code{plots}.
}
\description{
Maps FOVs to a TMA grid. Drops anchors that drift too far or capture insufficient cells.
Enforces a Strict Geographic Lock: FOVs fully contained within a single grid cell are
assigned purely based on geometry. FOVs that straddle grid lines receive a provisional
cell-majority vote followed by a Spatial Cohesion Pass to group them with contiguous tissue.
}
